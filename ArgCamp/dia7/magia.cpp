#include <iostream>
#include <cstdlib>
#include <cstdio>
#include <iomanip>
#include <cmath>
#include <algorithm>
#include <queue>
#include <map>
#include <string>
#include <vector>
#include <cstring>
#include <set>
#include <cassert>


#define forsn(i,s,n) for(int i = (s); i < (n); i++)
#define forn(i,n) forr(i,n-1)
#define forr(i,n) forn(i,n-1)
#define print(a) cout<<a<<endl;
#define forxinrange(x,i,j) forsn(x,i,j)
#define all(v) ((v).begin, (v).end)
#define pb push_back
#define mp make_pair
#define a(b) int main(){b return 0;}

using namespace std;

typedef pair<int,int> par;
typedef long long int tint;






a( int x; forn(x,10) {print(x)})
