#include <bits/stdc++.h>
using namespace std;
typedef  long long tint;
#define forsn(i, s, n) for (int i = 0; i < n; i++)
#define forn(i, n) forsn(i, 0, n)

#define debug(v) //cerr << #v << ": " << v << endl;
int main(){
	ios::sync_with_stdio(0); cin.tie(0);

	tint n; cin >> n;
	vector<pair<tint, tint>> g;
	vector<tint>
}